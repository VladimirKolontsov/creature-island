package entities.creatures.fauna.herbivores;

import repository.CreatureInfo;
import repository.Limit;

public class Duck extends Herbivores {
    public Duck(CreatureInfo info, Limit limit) {
        super(info, limit);
    }
}
