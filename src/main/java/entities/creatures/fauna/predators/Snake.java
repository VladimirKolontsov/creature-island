package entities.creatures.fauna.predators;

import repository.CreatureInfo;
import repository.Limit;

public class Snake extends Predator {
    public Snake(CreatureInfo info, Limit limit) {
        super(info, limit);
    }
}
