package entities.creatures.flora;

import entities.creatures.Creature;
import repository.CreatureInfo;
import repository.Limit;

public class Plant extends Creature {
    public Plant(CreatureInfo info, Limit limit) {
        super(info, limit);
    }
}
